# Mattbusel Homebrew tap

Prebuilt command-line tools by [Matt Busel](https://github.com/Mattbusel), installed with Homebrew on macOS and Linux.

```sh
brew install mattbusel/tap/<tool>
```

Each formula downloads the signed-checksum release binary from the tool's GitHub Releases page. Formulas live in `Formula/`.
